tasks:
  - title: "V-1: Verify Docker Services Running"
    prompt: >
      Verify Docker infrastructure is healthy. RETRY UNTIL ALL CHECKS PASS.


      1. Run `docker ps` to check containers

      2. Verify medusa-postgres is running and healthy

      3. Verify medusa-redis is running and healthy

      4. Test database connection: docker exec medusa-postgres pg_isready

      5. Test Redis connection: docker exec medusa-redis redis-cli ping


      SUCCESS CRITERIA: All 5 checks must pass. If any fail, fix the issue and
      re-verify.

      DO NOT mark complete until ALL services are healthy.
    completed: true
    parallel_group: 1
  - title: "V-2: Run All Unit Tests"
    prompt: >
      Run all unit tests. RETRY UNTIL ALL TESTS PASS.


      1. cd backend && npm test

      2. cd storefront && npm test


      SUCCESS CRITERIA: ALL tests must pass with 0 failures.

      If any test fails, investigate the failure, fix it, and re-run until
      green.

      DO NOT mark complete until all tests pass.
    completed: true
    parallel_group: 1
  - title: "V-3: E2E - Verify Storefront Homepage"
    prompt: |
      Use Playwright MCP to verify storefront homepage. RETRY UNTIL VERIFIED.

      1. Navigate to http://localhost:3000
      2. Wait for page to fully load (wait for network idle)
      3. Take a screenshot
      4. Check browser console for errors (should be none critical)
      5. Verify page has content (not blank/error page)

      Use: playwright/browser_navigate, playwright/browser_snapshot,
      playwright/browser_console_messages, playwright/browser_take_screenshot

      SUCCESS CRITERIA: Page loads with content, no critical console errors.
      If page fails to load or shows errors, investigate and retry.
    completed: true
    parallel_group: 1
  - title: "V-8: E2E - Verify Admin Panel Loads"
    prompt: >
      Use Playwright MCP to verify admin panel is accessible. RETRY UNTIL
      VERIFIED.


      1. Navigate to http://localhost:9000/app

      2. Wait for page load

      3. Verify login page or dashboard loads (not error)

      4. Take screenshot

      5. Check console for errors


      Use: playwright/browser_navigate, playwright/browser_snapshot,

      playwright/browser_console_messages, playwright/browser_take_screenshot


      SUCCESS CRITERIA: Admin panel loads (login page or dashboard visible).

      If it fails, check if backend is running and retry.
    completed: true
    parallel_group: 1
  - title: "V-9: API - Verify Products Endpoint"
    prompt: >
      Verify the products API returns correct data. RETRY UNTIL API WORKS.


      1. Make GET request to http://localhost:9000/store/products

      2. Verify 200 status code

      3. Verify response has products array

      4. Verify at least 10 products exist (we seeded 13)

      5. Verify each product has: id, title, handle, variants

      6. Print sample product structure


      Use curl: curl -s http://localhost:9000/store/products | jq


      SUCCESS CRITERIA: 200 response with 10+ products, each with required
      fields.

      If API fails, check backend logs and retry.
    completed: true
    parallel_group: 1
  - title: "V-10: API - Verify Cart Creation"
    prompt: |
      Verify cart API works correctly. RETRY UNTIL CART WORKS.

      1. POST to http://localhost:9000/store/carts to create cart
      2. Verify 200/201 response with cart object
      3. Save cart_id from response
      4. GET the cart to verify it persists
      5. Print cart structure

      Use curl: curl -X POST http://localhost:9000/store/carts

      SUCCESS CRITERIA: Cart creates successfully and can be retrieved.
      If fails, check backend and retry.
    completed: true
    parallel_group: 1
  - title: "V-4: E2E - Verify Products Display"
    prompt: >
      Use Playwright MCP to verify products display. RETRY UNTIL VERIFIED.


      1. Navigate to http://localhost:3000

      2. Wait for products to load

      3. Check that product cards/grid is visible

      4. Verify at least one product shows: image, title, price

      5. Check network requests - verify /store/products API was called

      6. Take screenshot showing products


      Use: playwright/browser_navigate, playwright/browser_snapshot,

      playwright/browser_network_requests, playwright/browser_take_screenshot


      SUCCESS CRITERIA: Products grid visible with at least 1 product showing
      image, title, price.

      If products don't display, check API and frontend code, then retry.
    completed: false
    parallel_group: 2
  - title: "V-5: E2E - Verify Product Detail Page"
    prompt: >
      Use Playwright MCP to verify product detail page. RETRY UNTIL VERIFIED.


      1. Navigate to http://localhost:3000

      2. Click on first product to go to detail page

      3. Wait for page load

      4. Verify product image displays

      5. Verify product title and price show

      6. Verify Add to Cart button exists

      7. Check network for /store/products/:id API call

      8. Take screenshot


      Use: playwright/browser_navigate, playwright/browser_click,

      playwright/browser_snapshot, playwright/browser_take_screenshot


      SUCCESS CRITERIA: Product detail page shows image, title, price, and Add
      to Cart button.

      If navigation fails or content missing, investigate and retry.
    completed: false
    parallel_group: 2
  - title: "V-6: E2E - Verify Add to Cart Flow"
    prompt: >
      Use Playwright MCP to verify full add-to-cart flow. RETRY UNTIL VERIFIED.


      1. Navigate to a product page

      2. Click "Add to Cart" button

      3. Check network requests for cart API call (POST /store/carts)

      4. Verify cart drawer/icon updates with item count

      5. Open cart (click cart icon if drawer)

      6. Verify product appears in cart with correct info

      7. Take screenshot of cart with item


      This tests: UI interaction -> API call -> State update -> UI reflects
      change


      Use: playwright/browser_navigate, playwright/browser_click,

      playwright/browser_network_requests, playwright/browser_snapshot


      SUCCESS CRITERIA: Adding item to cart succeeds, cart shows item with
      correct details.

      If cart doesn't update, check cart store and API integration, then retry.
    completed: false
    parallel_group: 3
  - title: "V-7: E2E - Verify Cart Operations"
    prompt: >
      Use Playwright MCP to verify cart operations work. RETRY UNTIL VERIFIED.


      1. Start with item in cart (from V-6 or add one)

      2. Find quantity controls in cart

      3. Increase quantity - verify API call and UI update

      4. Decrease quantity - verify API call and UI update

      5. Remove item - verify API call

      6. Verify cart shows empty state

      7. Take screenshots at each step


      Use: playwright/browser_click, playwright/browser_network_requests,

      playwright/browser_snapshot, playwright/browser_take_screenshot


      SUCCESS CRITERIA: All cart operations work - quantity changes reflect in
      UI and API.

      If operations fail, investigate cart component and store, then retry.
    completed: false
    parallel_group: 3
