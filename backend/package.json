{
  "name": "kuwait-marketplace-backend",
  "version": "1.0.0",
  "description": "Medusa v2 backend for Kuwait marketplace",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "medusa develop",
    "build": "medusa build",
    "start": "medusa start",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "db:migrate": "medusa db:migrate",
    "db:generate": "medusa db:generate",
    "db:sync-links": "medusa db:sync-links"
  },
  "dependencies": {
    "twilio": "^5.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.7",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
